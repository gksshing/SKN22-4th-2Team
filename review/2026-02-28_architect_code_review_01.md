### 🔍 총평 (Architecture Review)
전반적인 아키텍처가 매우 견고하며, RAG 스태빌리티(타임아웃, 예외처리), 프롬프트 인젝션 방어, 비동기 논블로킹 전환 및 Docker 최소 권한 원칙 등 모든 핵심 검수 기준이 훌륭하게 구현되어 있습니다. 특히 AWS Secrets Manager와 OIDC를 활용한 보안 셋업이 모범적입니다.

### 🚨 코드 리뷰 피드백 (개발 에이전트 전달용)
*(아래 내용을 복사해서 Backend 또는 DevOps 에이전트에게 전달하세요)*

**[🔴 Critical: 치명적 결함 - 즉시 수정 필요]**
- 높은 수준의 보안 및 아키텍처 요구사항을 통과하여 치명적 결함은 발견되지 않았습니다.

**[🟡 Warning: 잠재적 위험 - 개선 권장]**
- `src/vector_db.py:207` - `_os.environ["HOME"] = "/tmp"` 임시 수정 로직은 멀티 스레드/비동기 환경에서 동시성 문제를 일으킬 수 있으므로, 초기 구동(App Lifespan) 시점에 한 번만 실행되도록 구조를 개선하거나, `HF_HOME` 등 관련 라이브러리 환경 변수만으로 완전히 분리하는 것이 안전합니다.
- `main.py` (프로젝트 루트) - 현재 `entrypoint.sh`에서 `src.api.main:app`을 실행하도록 변경되었고 전역 예외 처리 및 보안 미들웨어가 해당 파일에 구현되어 있기 때문에, 혼동을 피하기 위해 더 이상 사용되지 않는 루트 위치의 `main.py`는 삭제(DELETE/Deprecated)하는 것을 권장합니다.

**[🟢 Info: 클린 코드 및 유지보수 제안]**
- `src/vector_db.py:302` - 텍스트를 10,000자로 하드코딩하여 자르는 부분은 시스템 프롬프트나 `src/config.py`로 빼내어 관리(예: `MAX_METADATA_LENGTH`)하면 재사용성이 높아지고 하드코딩 제로(Zero Hardcoding) 원칙에 좀 더 부합할 수 있습니다.
- `Dockerfile:115` - Healthcheck URL이 파이썬 내장 `urllib`를 활용하여 구성되었습니다. 컨테이너 베이스 이미지 내부에 Curl이 없어도 작동하므로 매우 경제적이고 좋은 도커 컨벤션입니다.

### 💡 Tech Lead의 머지(Merge) 권고
- [x] 이대로 Main 브랜치에 머지해도 좋습니다.
- [ ] Critical 항목이 수정되기 전까지 머지를 보류하세요.
